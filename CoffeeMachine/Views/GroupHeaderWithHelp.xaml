<UserControl x:Class="CoffeeMachineWPF.Views.GroupHeaderWithHelp"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="30" d:DesignWidth="300">

    <StackPanel Orientation="Horizontal">
        <TextBlock Text="{Binding Title}" VerticalAlignment="Center"/>

        <Button x:Name="HelpButton" Content="?" Width="18" Height="18" Margin="8,0,0,0" Padding="0"
                VerticalAlignment="Center" Cursor="Hand" Click="HelpButton_Click"/>

        <Popup x:Name="HelpPopup" StaysOpen="False" PlacementTarget="{Binding ElementName=HelpButton}" Placement="Bottom" AllowsTransparency="True">
            <Border Background="White" BorderBrush="#CCC" BorderThickness="1" CornerRadius="4" Padding="8" MaxWidth="320">
                <StackPanel>
                    <TextBlock Text="{Binding HelpText}" TextWrapping="Wrap"/>
                    
                </StackPanel>
            </Border>
        </Popup>
    </StackPanel>
</UserControl>

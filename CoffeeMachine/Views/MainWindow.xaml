<Window x:Class="CoffeeMachineWPF.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CoffeeMachineWPF"
        xmlns:vm="clr-namespace:CoffeeMachineWPF.ViewModels"
        xmlns:views="clr-namespace:CoffeeMachineWPF.Views"
        mc:Ignorable="d"
        Title="Кофейная машина" Height="600" Width="900"
        WindowStartupLocation="CenterScreen">


    <Window.Resources>
        <Style x:Key="OperationListItemStyle" TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#E3F2FD"/>
                                <Setter Property="BorderBrush" Value="#2196F3"/>
                                <Setter Property="BorderThickness" Value="2"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F5F5F5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- ЛЕВАЯ ПАНЕЛЬ - СПИСОК ОПЕРАЦИЙ -->
        <Border Grid.Column="0" Background="#f8f9fa" BorderBrush="#dee2e6" BorderThickness="0,0,1,0">
            <StackPanel>
                <TextBlock Text="ОПЕРАЦИИ" 
                          FontWeight="Bold" 
                          FontSize="16"
                          Foreground="#495057"
                          Margin="20,20,20,15"
                          HorizontalAlignment="Center"/>

                <ListBox ItemsSource="{Binding Operations}"
                         SelectedItem="{Binding SelectedOperation}"
                         BorderThickness="0"
                         Background="Transparent"
                         ItemContainerStyle="{StaticResource OperationListItemStyle}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="15,12" 
                                    Background="Transparent"
                                    Margin="10,2">
                                <TextBlock Text="{Binding OperationName}"
                                          FontSize="14"
                                          Foreground="#212529"
                                          FontWeight="SemiBold"/>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </StackPanel>
        </Border>

        <!-- Выбранная операция -->
        <ContentControl Grid.Column="1" 
                       Content="{Binding SelectedOperation}"
                       Margin="20"/>
    </Grid>
</Window>